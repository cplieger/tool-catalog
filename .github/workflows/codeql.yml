name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  codeql:
    permissions:
      security-events: write
      contents: read
      actions: read
    uses: cplieger/ci/.github/workflows/codeql.yaml@77bb6658a592401093bfa4bf7118081700e0d9ff # v2
