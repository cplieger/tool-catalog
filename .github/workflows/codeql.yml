name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  codeql:
    permissions:
      security-events: write
      contents: read
      actions: read
    uses: cplieger/ci/.github/workflows/codeql.yaml@68cf3b50758d7d46f35dbfadb05042048a1b3e6b # v2
