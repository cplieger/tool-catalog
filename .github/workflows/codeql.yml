name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  codeql:
    permissions:
      security-events: write
      contents: read
      actions: read
    uses: cplieger/ci/.github/workflows/codeql.yaml@b899b20c6ed98b09df0a8a506f74092718eec445 # v2
